nijou(n) {
  return n*n;
}

sum(n){
  var i, sum;
  i := 1;
  sum := 0;
  
  while i <= n do {
    sum := sum + nijou(i);
    i := i + 1;
  }
  return sum;
}

main {
  var n;
  read n;
  write sum(n);
  writeln;
}